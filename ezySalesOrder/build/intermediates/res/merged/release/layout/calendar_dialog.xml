<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/myLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <CalendarView
        android:id="@+id/calendarID"
        android:background="#1EBCF5"
        android:dateTextAppearance="@android:style/TextAppearance.Medium"
        android:focusedMonthDateColor="@drawable/header_bg"
        android:selectedDateVerticalBar="@color/white"
        android:selectedWeekBackgroundColor="#1EBCF5"
        android:layout_width="match_parent"
        android:layout_height="350dip"/>
</FrameLayout>